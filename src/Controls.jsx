import React from 'react';

const Controls = () => (
    <div>
        <button type="button">Start</button>
        <button type="button">Take lap</button>
        <button type="button">Restart</button>
    </div>
);

export default Controls;
